import React from 'react';
import UserNavbar from '../../components/UserNavbar';

const UserDashboard = () => {
    return (
        <div>
            <UserNavbar />
        </div>
    );
};

export default UserDashboard;
