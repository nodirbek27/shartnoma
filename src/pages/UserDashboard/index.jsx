import React from 'react';
import User from '../../components/User';

const UserDashboard = () => {
    return (
        <div>
            <User />
        </div>
    );
};

export default UserDashboard;
